import XCTest
@testable import SwiftPackageForBluetoothForDataTransfer

final class SwiftPackageForBluetoothForDataTransferTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftPackageForBluetoothForDataTransfer().text, "Hello, World!")
    }
}
